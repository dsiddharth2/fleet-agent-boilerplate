services:
  fleet:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
    # Live runs: entrypoint starts Fleet, registers members, then
    # apra-fleet auth from CLAUDE_CODE_OAUTH_TOKEN or .token
    environment:
      CLAUDE_CODE_OAUTH_TOKEN: ${CLAUDE_CODE_OAUTH_TOKEN:-}
